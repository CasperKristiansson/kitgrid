# Terraform notes

Personal stack for kitgrid.dev + *.kitgrid.dev. Run `terraform init/plan/apply` from this folder with `AWS_PROFILE=Personal` (or override via env). Vars like domains and aliases are already in `locals.tf`; adjust inline when needed. No public instructions.
