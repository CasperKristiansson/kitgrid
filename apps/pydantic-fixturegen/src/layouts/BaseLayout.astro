---
import '../styles/global.css';
import DocsNavbar from '@kitgrid/docs-ui/components/DocsNavbar.astro';
import { project } from '../data/project';

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  keywords?: string;
  structuredData?: Record<string, any>;
}

const {
  title = 'Project docs',
  description = 'Docs preview for kitgrid.',
  canonical,
  ogImage = '/assets/pydantic-fixturegen-logo.png',
  keywords = 'pydantic fixturegen, deterministic test data, python fixtures, JSON emitters, pytest',
  structuredData,
} = Astro.props;
const isProd = import.meta.env.PROD;
const siteOrigin = Astro.site?.origin ?? 'https://pydantic-fixturegen.kitgrid.dev';
const derivedCanonical =
  canonical ?? new URL(Astro.url?.pathname ? Astro.url.pathname : '/', siteOrigin).href;
const resolvedOgImage = new URL(ogImage, siteOrigin).toString();
const defaultStructuredData = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'Pydantic Fixturegen',
  applicationCategory: 'DeveloperApplication',
  operatingSystem: 'Python 3.11+',
  url: derivedCanonical,
  description,
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
};
const schemaToRender = structuredData ?? defaultStructuredData;
const theme = project.theme ?? {};
const themeCss = `
:root {
  --theme-primary: ${theme.primary ?? '#1D4ED8'};
  --theme-secondary: ${theme.secondary ?? '#1D4ED8'};
  --theme-bg: ${theme.bg ?? '#FFFFFF'};
  --theme-surface: ${theme.surface ?? '#F5F7FB'};
  --theme-text: ${theme.text ?? '#0B0F14'};
  --theme-muted: ${theme.muted ?? '#6B7280'};
  --theme-link: ${theme.link ?? '#1D4ED8'};
  --theme-code: ${theme.code_theme === 'kitgrid-light' ? '#0F172A' : '#0F172A'};
  --motion-scale: ${theme.motion === 'subtle' ? '0.85' : '1'};
  --anim-duration: calc(220ms * var(--motion-scale));
}
`;

const csp = [
  "default-src 'self'",
  "script-src 'self' 'unsafe-inline' data: blob:",
  "style-src 'self' 'unsafe-inline'",
  "img-src 'self' data: https://img.shields.io",
  "font-src 'self' data:",
  "connect-src 'self'",
  "object-src 'none'",
  "base-uri 'self'",
  "form-action 'self'",
].join('; ');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0A0F14" />
    <meta name="keywords" content={keywords} />
    <link rel="canonical" href={derivedCanonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={derivedCanonical} />
    <meta property="og:image" content={resolvedOgImage} />
    <meta property="og:type" content="article" />
    <meta property="og:site_name" content="Pydantic Fixturegen Docs" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={resolvedOgImage} />
    {isProd && <meta http-equiv="Content-Security-Policy" content={csp} />}
    {schemaToRender && (
      <script type="application/ld+json">
        {JSON.stringify(schemaToRender)}
      </script>
    )}
    <style is:inline>{themeCss}</style>
    <slot name="head" />
    <link rel="icon" type="image/png" sizes="128x128" href="/favicon.png" />
  </head>
  <body data-theme="pydantic-fixturegen">
    <DocsNavbar
      title="pydantic-fixturegen"
      logoSrc="/assets/pydantic-fixturegen-logo.png"
      logoAlt="Pydantic Fixturegen logo"
      repoUrl="https://github.com/CasperKristiansson/pydantic-fixturegen"
      links={[
        { label: 'Quickstart', href: '/docs/quickstart/' },
        { label: 'CLI', href: '/docs/cli/' },
        { label: 'API', href: '/docs/api/' },
        { label: 'Install', href: '/docs/install/' },
      ]}
      searchRemoteSrc={import.meta.env.DEV ? 'https://pydantic-fixturegen.kitgrid.dev/pagefind/pagefind.js' : undefined}
    />
    <main>
    <slot />
    </main>
  </body>
</html>
